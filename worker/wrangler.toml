name = "fibonacci-mcfibface-api"
main = "src/index.ts"
compatibility_date = "2024-12-01"

[[durable_objects.bindings]]
name = "ROOMS"
class_name = "RoomDO"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["RoomDO"]

# Keep worker warm with periodic pings (every 5 minutes)
[triggers]
crons = ["*/5 * * * *"]
